CREATE DATABASE myDB; --creates a new database

USE myDb; --uses the "myDb" database

DROP DATABASE myDb; --deletes a database

--change DATABASE
ALTER DATABASE myDb READ ONLY = 1; --this will change the data base to ready only. you can't change or delete database but you can sill get the data